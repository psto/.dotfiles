git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never --diff-so-fancy
customCommands:
  - key: '<c-space>'
    command: 'git diff --color-words {{.SelectedFile.Name}}'
    context: 'global'
    subprocess: true
